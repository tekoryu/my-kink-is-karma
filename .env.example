# ==============================================================================
# CORE DJANGO SETTINGS
# ==============================================================================
# SECURITY WARNING: keep the secret key used in production secret!
# This is a generated key for local development.
SECRET_KEY='django-insecure-a8!b$v&z+q@w9x*y2z(c3d)e4f!g5h^i6j&k7l(m8n'

# SECURITY WARNING: don't run with debug turned on in production!
# Set to True for development to get detailed error pages.
DEBUG=True

# Hosts/domain names that this Django site can serve.
# For development, 'localhost' and '127.0.0.1' are usually sufficient.
ALLOWED_HOSTS=localhost,127.0.0.1

# ==============================================================================
# DATABASE (POSTGRESQL) SETTINGS
# ==============================================================================
# These variables are typically used by a Docker Compose setup.
# The database service in docker-compose.yml would be named 'db'.
POSTGRES_DB=agenda-prioritaria
POSTGRES_USER=tekoryu
POSTGRES_PASSWORD=senhasenha!@#
POSTGRES_HOST=db
POSTGRES_PORT=5432

# This is the Data Source Name (DSN) that Django will use to connect.
# It's constructed from the variables above.
DATABASE_URL=postgres://tekoryu:strongpassword123!@#@db:5432/my_django_project_db

TIME_ZONE=UTC

